// The archive, contact links and studio are served by this same deployment
// (see /api), so the base is same-origin — which is also what lets the studio's
// session cookie work without any cross-site cookie handling.
export const API = "/api";
